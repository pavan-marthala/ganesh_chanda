import 'package:flutter/material.dart';

class FestivalsSetupScreen extends StatelessWidget {
  const FestivalsSetupScreen({super.key, required this.id});
  final String id;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Column(children: [])),
    );
  }
}
